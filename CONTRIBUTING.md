# 参与贡献

## 开始任务

1. 在 Issue 中确认负责人、范围、依赖和验收标准。
2. 若改动跨模块，先由相关模块负责人确认接口边界。
3. 从最新 `main` 建立短分支，不直接向 `main` 推送功能代码。

## 提交与 PR

- 提交信息使用简短动词开头，例如 `add protocol command example`。
- PR 标题建议使用 `scope: summary`，例如 `protocol: define device command envelope`。
- PR 正文必须关联 Issue，并写明变更、验证方式和硬件影响。
- 不提交 API Key、Wi-Fi 密码、证书、模型权重、构建产物或个人数据。
- 真实硬件测试要记录开发板、固件版本、接线和结果。

## 完成定义

- 需求和验收标准均已满足。
- 相关文档与协议示例已同步。
- 本地检查通过；硬件相关改动附手工测试证据。
- 没有把无关格式化或重构混入 PR。

## 协议变更

协议是固件和服务端的共同边界。任何不向后兼容的变更都必须：

1. 新增或提升协议版本；
2. 更新 `protocol/schema/` 与 `protocol/examples/`；
3. 说明固件端和服务端的迁移顺序；
4. 同时请求两端成员审核。
